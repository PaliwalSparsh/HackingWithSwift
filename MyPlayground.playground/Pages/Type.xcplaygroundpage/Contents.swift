//: [Previous](@previous)

import Foundation

// 1. We can declare a constant before hand and then assign a value to it later, however once assigned that value cannot be changed.
let username:String

// some complex code

username = "Sparsh"

// more complex code

print(username)

// HackingwithSwift: my own code, I prefer to use type inference as much as possible. That means I leave off the type annotations, and let Swift figure out the type of things based on what data I store in them.
